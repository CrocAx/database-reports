SELECT Name
FROM Student
JOIN Enrollment ON Student.StudentID = Enrollment.StudentID
WHERE Enrollment.CourseID = '1';

SELECT Name
FROM Student
JOIN Enrollment ON Student.StudentID = Enrollment.StudentID
WHERE Enrollment.CourseID = '2';

SELECT Name
FROM Student
JOIN Enrollment ON Student.StudentID = Enrollment.StudentID
WHERE Enrollment.CourseID = '3';

SELECT Name
FROM Student
JOIN Enrollment ON Student.StudentID = Enrollment.StudentID
WHERE Enrollment.CourseID = '4';

SELECT Name
FROM Student
JOIN Enrollment ON Student.StudentID = Enrollment.StudentID
WHERE Enrollment.CourseID = '5';

SELECT Name
FROM Student
JOIN Enrollment ON Student.StudentID = Enrollment.StudentID
WHERE Enrollment.CourseID = '6';

SELECT Name
FROM Student
JOIN Enrollment ON Student.StudentID = Enrollment.StudentID
WHERE Enrollment.CourseID = '7';

SELECT Name
FROM Student
JOIN Enrollment ON Student.StudentID = Enrollment.StudentID
WHERE Enrollment.CourseID = '8';
